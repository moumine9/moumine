{
  "private": true,
  "name": "moumine9",
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "fetch:projects": "node scripts/fetch-projects.mjs",
    "render:case-studies": "node scripts/render-case-studies.mjs",
    "prebuild:data": "npm run fetch:projects && npm run render:case-studies",
    "build": "npm run prebuild:data && vite build && node scripts/inject-titles.mjs",
    "dev": "npm run prebuild:data && vite",
    "preview": "vite preview"
  },
  "dependencies": {
    "dayjs": "^1.11.7",
    "preact": "^10.19.5",
    "preact-iso": "^2.4.0",
    "preact-render-to-string": "^6.3.1",
    "preact-router": "^3.2.1",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "@types/node": "^26.2.0",
    "sirv-cli": "^2.0.2",
    "typescript": "^5.9.3",
    "vite": "^8.2.1"
  }
}
